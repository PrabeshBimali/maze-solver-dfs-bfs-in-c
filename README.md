# Maze Solver DFS And BFS

**Use Following command to generate maze_program binary and Run it.**
```bash
gcc -Wall -Wextra -std=c99 main.c path_finder.c maze_image_generator.c -o maze_program

./maze_program
```
**Note: Please read and manipulate `main.c` file for adding your own custom maze**
